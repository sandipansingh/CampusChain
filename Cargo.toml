[workspace]
resolver = "2"
members = [
    "contracts/campus-token",
    "contracts/campus-service",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
lto = true
codegen-units = 1

[profile.release-with-logs]
inherits = "release"
debug = true
